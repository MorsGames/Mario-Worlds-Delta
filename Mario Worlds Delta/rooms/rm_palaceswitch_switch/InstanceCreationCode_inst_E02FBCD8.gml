/// @description Set up the sprite based on the last room

if (obj_controller.last_room == rm_palaceswitch_y)
    sprite_index = spr_palaceswitch_y;
else if (obj_controller.last_room == rm_palaceswitch_g)
    sprite_index = spr_palaceswitch_g;
else if (obj_controller.last_room == rm_palaceswitch_r)
    sprite_index = spr_palaceswitch_r;
else if (obj_controller.last_room == rm_palaceswitch_b)
    sprite_index = spr_palaceswitch_b;
